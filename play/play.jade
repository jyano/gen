include mixins

!!! 5
html(lang="en")
    head
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        script.
          _app = wappyApp = '#{app}'
          _pam = wappyPam = '#{pam}'


        //front
        +S(['us','string','both', 'jq','objects','qq','jqfn','html'])
        //can
        +S(['ctx', 'can','canApps'])
        //create
        +S(['cjs','cjsMC'])
        +S(['easel', 'cjsEvent' ,'easelProtos','easelProtosMore','create','graphicsMore','filts'])
        //anim
        +S(['data','anims', 'anim', 'graphicsTests'])


        //fiz
        +S(['bx','gpcas','box', 'fiz', 'query','coll'])
        +S(['scrolling', 'animFiz', 'bindSpriteBm', 'pic',  'spaz'    ])
        

        +S(['joints', 'distJoints', 'revJoints', 'advancedJoints'])
        +S(['geo', 'co'])
        +S(['player'])
        +S(['betaGames','coGames', 'cxGames','geoGames'])
        +S(['jointGames','netguy','scrollGames', 'shapePuzGames', 'yipGames'])
        +S(['play'])
        body